export const config = {
  apiKey: "{{apiKey}}",
  authDomain: "{{authDomain}}",
  databaseURL: "{{databaseURL}}",
  projectId: "{{projectId}}",
  storageBucket: "",
  messagingSenderId: "{{messagingSenderId}}"
};
